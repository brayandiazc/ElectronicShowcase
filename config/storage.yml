test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# Use bin/rails credentials:edit to set the AWS secrets (as aws:access_key_id|secret_access_key)
amazon:
  service: S3
  access_key_id: AKIA2Q42ESZVLXL3I6VC
  secret_access_key: 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
  region: us-east-1
  bucket: mascot-tinder
